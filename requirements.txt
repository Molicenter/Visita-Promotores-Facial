# --- Base (mesmo padrão dos outros apps Molicenter) ---
# ATENCAO: o streamlit precisa ser 1.5x. O 1.38 exige protobuf<6 e o
# tensorflow 2.21 exige protobuf>=6.31 — nao existe combinacao que
# resolva com os dois. Foi isso que quebrou o deploy de 08/09/2026.
streamlit==1.59.2
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
requests==2.32.3
openpyxl==3.1.5
pytz==2025.2
Pillow==11.3.0
supabase==2.31.0

# --- Reconhecimento facial (TF e tf-keras SEMPRE na mesma versão) ---
tensorflow-cpu==2.21.0
tf-keras==2.21.0
deepface==0.0.100

# O deepface e o retina-face puxam o "opencv-python" normal. Sem pin,
# vinha o opencv 5.0 (novissimo, API diferente). Travado em 4.11 pra
# casar com a headless. A headless fica por ULTIMO de proposito: ela e
# instalada depois e o cv2 que sobra e o que NAO precisa da libGL.so.1
# do sistema — assim o app roda sem packages.txt nenhum.
opencv-python==4.11.0.86
opencv-python-headless==4.11.0.86