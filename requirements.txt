# --- Base (mesmo padrão dos outros apps Molicenter) ---
streamlit==1.38.0
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
requests==2.32.3
openpyxl==3.1.5
pytz==2025.2
Pillow==11.3.0
supabase==2.31.0

# --- Reconhecimento facial (TF e tf-keras SEMPRE na mesma versão) ---
tensorflow-cpu==2.21.0
tf-keras==2.21.0
deepface==0.0.100

# ATENÇÃO: o opencv-python-headless fica por ULTIMO de proposito.
# O deepface puxa o "opencv-python" normal como dependencia, e esse
# exige a libGL.so.1 do sistema (que so vinha pelo packages.txt).
# Instalando a versao headless depois, o cv2 que fica valendo e o que
# NAO precisa de libGL — assim o app roda sem packages.txt nenhum.
opencv-python-headless==4.11.0.86